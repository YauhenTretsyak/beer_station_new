import styled from 'styled-components'
import {SectionContainer, SectionTitle} from '../../styles/StyledElements'

export const ProductContainer = styled(SectionContainer)``
export const ProductTitle = styled(SectionTitle)``
